import { PopularTravelersSection } from "./popular-travelers";

/** Back-compat wrapper for the existing community home import. */
export default async function FeaturedTravelers() {
  return <PopularTravelersSection />;
}
