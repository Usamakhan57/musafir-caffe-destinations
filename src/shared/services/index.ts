/**
 * Shared services — cross-feature business operations built on top of
 * the API client layer (`@/lib/api-client`). Services used by exactly
 * one feature should live inside that feature folder instead.
 *
 * Re-export services from this barrel:
 *   export { menuService } from "./menu-service";
 */
export {};
