/**
 * Shared React hooks reusable across features
 * (useDebounce, useMediaQuery, useLocalStorage, …).
 *
 * Re-export hooks from this barrel:
 *   export { useDebounce } from "./use-debounce";
 */
export {};
